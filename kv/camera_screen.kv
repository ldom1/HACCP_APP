<CameraScreen>:
    CameraClick:
        orientation: 'vertical'
        Camera:
            id: camera
            resolution: self.size
            play: True
        Button:
            text: 'Capture'
            size_hint_y: None
            height: '48dp'
            on_press:
                root.capture()